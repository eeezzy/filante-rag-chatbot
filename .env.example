ANTHROPIC_API_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# Required in any environment reachable over the public internet — requests
# without a matching X-API-Key header are rejected. Generate with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
API_SHARED_SECRET=
